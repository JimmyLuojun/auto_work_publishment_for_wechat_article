[WeChat]
default_author = Your Official Account Name
# Optional: Specify cover image behavior (e.g., 'first', 'none', or path marker)
cover_image_strategy = first
# Optional: Default comment settings (0=off, 1=on)
need_open_comment = 1
only_fans_can_comment = 0

[Paths]
# Optional: Default input/output if not provided elsewhere
# input_dir = data/input
output_dir = data/output
log_file = app.log

[Logging]
level = INFO # DEBUG, INFO, WARNING, ERROR, CRITICAL